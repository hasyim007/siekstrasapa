{
  "name": "survei-ekskul-mpa-cloudflare",
  "private": true,
  "version": "1.0.0",
  "description": "Survei Ekskul SDN 01 Papahan - versi Cloudflare Workers + Static Assets + D1",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:schema": "wrangler d1 execute siekstrasapa2 --remote --file=./schema.sql",
    "db:schema:local": "wrangler d1 execute siekstrasapa2 --local --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
